const firstName = "Aaron";

const lastname = "Thompson";

const usesReact = true;

const fullName = `${firstName} ${lastName}`;

const summary = `${fullName} ${usesReact ? "usesReact" : "does not use react"}`;
